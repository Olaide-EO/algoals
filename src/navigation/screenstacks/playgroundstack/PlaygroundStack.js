import React from "react";
import Playground from "../../../screens/playground/Playground";

const PlaygroundStack = ({route}) => {
  return (
   <Playground {...{route}}/>
  )
};

export default PlaygroundStack;
